% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_input_first.R
\name{analyze_missingness}
\alias{analyze_missingness}
\title{Build an Experimental Missingness Analysis Sidecar}
\usage{
analyze_missingness(
  x,
  design,
  assay = NULL,
  base_fit = NULL,
  cutoffs = NULL,
  seed = 1L,
  modules = c("sentinel", "stability")
)
}
\arguments{
\item{x}{An ordinary numeric matrix or a \code{SummarizedExperiment} containing
unnormalised log2 protein intensities with features in rows and samples in
columns.}

\item{design}{A \code{\link[=missingness_design]{missingness_design()}} with sample names exactly matching
\code{x}. For \code{SummarizedExperiment} input, its condition role must also name an
equal \code{colData} column.}

\item{assay}{Assay name for \code{SummarizedExperiment} input. It may be omitted
only when exactly one assay exists. It must be \code{NULL} for matrix input.}

\item{base_fit}{Optional complete \code{imputefinder_result}. Its cutoff-source
policy is recomputed against \code{x}, \code{design}, and \code{seed}; reuse occurs only
when exact, canonical, and tolerance comparisons all pass.}

\item{cutoffs}{Optional named numeric vector of manual condition cutoffs,
with automatic selection filling omitted conditions. \code{base_fit} and
\code{cutoffs} are conflicting specifications.}

\item{seed}{Integer seed for the classic condition-rescue path.}

\item{modules}{Unique subset of \code{"sentinel"} and \code{"stability"}. The default
constructs pre-rescue evidence plus static coverage and requests
stability. Stability currently returns a structured
\code{imputefinder_unavailable} record pending its frozen validation milestone.
Use \code{character()} to skip both modules.}
}
\value{
An experimental \code{imputefinder_analysis} with seven fields:
\code{classic}, \code{spec}, \code{design}, \code{input}, \code{sentinel}, \code{stability}, and
\code{provenance}. \code{design$estimability} stores the canonical declared model,
SVD rank/null-space aliases, and block-aware independent-unit accounting.
The sidecar retains computed evidence and a packed missingness mask, but
does not retain a second copy of the original numeric matrix.
}
\description{
Construct an input-first companion analysis without changing the stable
\code{\link[=classify_missingness]{classify_missingness()}} result contract. The sidecar records the original
missingness mask, a deterministic input fingerprint, aligned typed design,
algebraic design/estimability evidence, pre-rescue evidence, static design
coverage, classic result or structured failure, and provenance.
}
\details{
The sidecar reports observed evidence, compatibility, and unavailable
quantities. It does not identify a causal missingness mechanism. Serialized
objects carry an experimental versioned schema and unknown schemas fail
explicitly instead of being silently upgraded.

The mandatory design core runs independently of \code{modules}. It reports
algebraic separability and declared units; it performs no association test,
automatic correction, sample exclusion, or causal attribution.

Selected sentinel output contains \code{pre_rescue} and \code{coverage} records.
Coverage uses globally observable features - features finite in at least one
original cell - as its detection denominator while retaining the full input
feature count. It stores canonical sample and condition support, all
declared role levels with singleton flags, complete condition-by-nuisance,
block, and acquisition grids including zero-sample cells, and pairwise
condition feature overlap. These are descriptive pre-rescue summaries, not
association tests or causal evidence.
}
\examples{
x <- matrix(
    seq_len(16),
    nrow = 4,
    dimnames = list(paste0("protein", 1:4), paste0("sample", 1:4))
)
design <- missingness_design(
    data.frame(
        condition = c("control", "control", "treated", "treated"),
        row.names = colnames(x)
    ),
    condition = "condition"
)

analysis <- analyze_missingness(x, design, modules = "sentinel")
analysis

}
\seealso{
\code{\link[=missingness_design]{missingness_design()}} and \code{\link[=classify_missingness]{classify_missingness()}}.
}
